/**
 * If `type` is a string, check whether `value` has that type. Other kinds will
 * check that the types of `type` and `value` match.
 */
export default function isType(value: unknown, type: string): boolean;
