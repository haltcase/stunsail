/**
 * Check whether two values `a` and `b` are deeply equal. Works on almost any
 * object - including plain Objects, Arrays, Maps, Sets, and Dates.
 */
export default function is<T>(a: T, b: T): boolean;
